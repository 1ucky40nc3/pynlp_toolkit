name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Thanks for collaborating! Keep the spirit up! 😉'
        pr-message: 'Thanks for collaborating! Keep the spirit up! 😉'
